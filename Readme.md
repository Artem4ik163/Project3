# 💳 Полноценный Bank API

REST API для банковских операций с поддержкой **баланса**, **истории транзакций** и **переводов между пользователями**.  
Данные хранятся в **PostgreSQL**, все операции атомарны благодаря **транзакциям**.

---

## 📌 Реализованные операции

| Метод | Endpoint | Параметры | Описание |
|------|----------|----------|--------|
| `GET` | `/api/bank/getBalance` | `userId` | Получить баланс |
| `POST` | `/api/bank/putMoney` | `userId`, `amount` | Пополнить счёт |
| `POST` | `/api/bank/takeMoney` | `userId`, `amount` | Снять деньги |
| `POST` | `/api/bank/transferMoney` | `currentUserId`, `targetUserId`, `amount` | Перевести деньги |
| `GET` | `/api/bank/getOperationList` | `userId`, `from` (опц.), `to` (опц.) | История операций |

> 📅 Формат даты: `yyyy-MM-ddTHH:mm:ss` (например: `2026-01-03T15:30:00`)

---

## 🗃 Структура базы данных

### Таблица `accounts`
Хранит баланс пользователей.

### Таблица `transactions`
Хранит все операции.

![Структура базы данных](screenshots/db-structure.png)

> 💡 Скриншот сделан в pgAdmin 4.  
> (Файл: `screenshots/db-structure.png`)

---

## 💾 Dump базы данных

SQL-дамп со структурой таблиц включён в репозиторий:  
📁 [`dumps/bank_db_schema.sql`](dumps/bank_db_schema.sql)

Чтобы создать БД из дампа:
```bash
createdb bank_db
psql -U postgres -d bank_db -f dumps/bank_db_schema.sql